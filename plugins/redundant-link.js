export function fn(dom) {
    let output = "Error: redundant link ";
    let errors = 0;

    // TODO: function body for plugin 5

    if (errors === 0) return ["", errors];
    return [output, errors];
}