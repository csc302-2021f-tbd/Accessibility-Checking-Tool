export function fn(dom) {
    let output = "Error: missing/empty link text ";
    let errors = 0;

    // TODO: function body for plugin 4

    if (errors === 0) return ["", errors];
    return [output, errors];
}