export function fn(dom) {
    let output = "Error: missing alternate image text ";
    let errors = 0;

    // TODO: function body for plugin 3

    if (errors === 0) return ["", errors];
    return [output, errors];
}