export function fn(dom) {
    let output = "Error: use of justified text alignment ";
    let errors = 0;

    // TODO: function body for plugin 10

    if (errors === 0) return ["", errors];
    return [output, errors];
}