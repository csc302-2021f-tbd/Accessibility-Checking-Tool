export function fn(dom) {
    let output = "Error: mismatch between element label and accessibility name ";
    let errors = 0;

    // TODO: function body for plugin 11

    if (errors === 0) return ["", errors];
    return [output, errors];
}